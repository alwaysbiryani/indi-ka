# Deployment Architecture - Indi-क Mobile-First UI

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    DEVELOPER WORKFLOW                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ git push origin main
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                  GITHUB REPOSITORY (main branch)                 │
│              https://github.com/alwaysbiryani/indi-ka            │
│                                                                   │
│  ✅ Single page.tsx (mobile-first only)                          │
│  ✅ No legacy UI code                                            │
│  ✅ Version stamping in layout.tsx                               │
│  ✅ Build verification in next.config.ts                         │
└─────────────────┬───────────────────────────┬───────────────────┘
                  │                           │
                  │                           │
                  ▼                           ▼
┌─────────────────────────────────┐  ┌──────────────────────────┐
│     GITHUB ACTIONS (CI/CD)      │  │   VERCEL AUTO-DEPLOY     │
│  .github/workflows/              │  │   (vercel.json)          │
│  build-verification.yml          │  │                          │
│                                  │  │  Locked to main branch   │
│  🔍 Scan for legacy patterns     │  │  only                    │
│  🔍 Verify single page.tsx       │  │                          │
│  🏗️  Build application           │  │  Triggered by push to    │
│  📦 Verify build output          │  │  main                    │
│  ✅ Pass/Fail gate               │  │                          │
└─────────────────┬───────────────┘  └──────────┬───────────────┘
                  │                              │
                  │ ✅ All checks pass           │
                  │                              │
                  └──────────────┬───────────────┘
                                 │
                                 ▼
                  ┌──────────────────────────────┐
                  │    VERCEL BUILD PROCESS      │
                  │                              │
                  │  1. npm ci                   │
                  │  2. npm run build            │
                  │  3. Generate BUILD_ID        │
                  │     (from git commit SHA)    │
                  │  4. Optimize assets          │
                  │  5. Deploy to Edge Network   │
                  └──────────────┬───────────────┘
                                 │
                                 │ Build complete
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PRODUCTION DEPLOYMENT                         │
│                  https://indi-ka.vercel.app/                     │
│                                                                   │
│  📱 Mobile-First UI Only                                         │
│  🏷️  Build Metadata in HTML <head>                              │
│     • build-commit: <git-sha>                                    │
│     • build-time: <iso-timestamp>                                │
│     • ui-version: mobile-first-v2                                │
│                                                                   │
│  🖥️  Console Output:                                             │
│     🇮🇳 Indi-क Mobile-First UI                                   │
│     Build Commit: abc1234                                        │
│     Build Time: 2026-02-11T...                                   │
│     UI Version: mobile-first-v2                                  │
│                                                                   │
│  🌐 Response Headers:                                            │
│     X-Build-Source: github-main-mobile-first                     │
│     Cache-Control: public, max-age=0, must-revalidate            │
│                                                                   │
│  ⚡ Vercel Edge Network (Global CDN)                             │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 │
                                 ▼
                  ┌──────────────────────────────┐
                  │      END USERS               │
                  │                              │
                  │  🌍 Global access            │
                  │  📱 Mobile-first experience  │
                  │  ⚡ Fast edge delivery       │
                  │  🔒 No legacy UI possible    │
                  └──────────────────────────────┘
```

---

## Safety Guard Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                        LAYER 1: SOURCE CODE                      │
│  • No legacy UI code exists (verified)                           │
│  • Single page.tsx serves mobile-first UI                        │
│  • No feature flags or conditional rendering                     │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                     LAYER 2: LOCAL VERIFICATION                  │
│  • npm run verify:ui - Scans for legacy patterns                │
│  • npm run verify:build - Ensures clean build                   │
│  • npm run deploy:check - Pre-deployment gate                   │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                      LAYER 3: CI/CD AUTOMATION                   │
│  • GitHub Actions workflow on every push                         │
│  • Automated legacy pattern detection                            │
│  • Build verification before merge                               │
│  • Fails fast if issues detected                                 │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                   LAYER 4: DEPLOYMENT LOCK                       │
│  • vercel.json locks production to main branch                   │
│  • No preview branches can deploy to production                  │
│  • GitHub is the single source of truth                          │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    LAYER 5: BUILD VERIFICATION                   │
│  • Build ID generated from git commit SHA                        │
│  • Clean builds enforced (no stale artifacts)                    │
│  • Strict mode enabled                                           │
│  • Build time stamped                                            │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                   LAYER 6: RUNTIME VERIFICATION                  │
│  • Version metadata in HTML <head>                               │
│  • Console logging on app load                                   │
│  • Response headers identify build source                        │
│  • Instant verification in production                            │
└─────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                     LAYER 7: CACHE CONTROL                       │
│  • Cache-Control headers prevent stale UI                        │
│  • Asset hashing ensures fresh bundles                           │
│  • CDN invalidation on every deploy                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## Deployment Flow Sequence

```
Developer                GitHub              GitHub Actions       Vercel              Production
    │                       │                       │                │                    │
    │  git push main        │                       │                │                    │
    ├──────────────────────>│                       │                │                    │
    │                       │                       │                │                    │
    │                       │  Trigger workflow     │                │                    │
    │                       ├──────────────────────>│                │                    │
    │                       │                       │                │                    │
    │                       │                       │  Scan for      │                    │
    │                       │                       │  legacy UI     │                    │
    │                       │                       │  ✅ Pass       │                    │
    │                       │                       │                │                    │
    │                       │                       │  Build app     │                    │
    │                       │                       │  ✅ Success    │                    │
    │                       │                       │                │                    │
    │                       │  ✅ Checks passed     │                │                    │
    │                       │<──────────────────────┤                │                    │
    │                       │                       │                │                    │
    │                       │  Trigger deploy       │                │                    │
    │                       ├───────────────────────────────────────>│                    │
    │                       │                       │                │                    │
    │                       │                       │                │  npm ci            │
    │                       │                       │                │  npm run build     │
    │                       │                       │                │  Generate BUILD_ID │
    │                       │                       │                │  Deploy to Edge    │
    │                       │                       │                │                    │
    │                       │                       │                │  ✅ Deployed       │
    │                       │                       │                ├───────────────────>│
    │                       │                       │                │                    │
    │  ✅ Deployment ready  │                       │                │                    │
    │<──────────────────────┴───────────────────────┴────────────────┴────────────────────┤
    │                                                                                      │
    │  Open browser                                                                        │
    │  https://indi-ka.vercel.app/                                                        │
    ├─────────────────────────────────────────────────────────────────────────────────────>│
    │                                                                                      │
    │  🇮🇳 Mobile-First UI                                                                 │
    │  Build: abc1234                                                                      │
    │<─────────────────────────────────────────────────────────────────────────────────────┤
```

---

## Rollback Flow

```
Issue Detected                Git                 Vercel              Production
      │                       │                     │                    │
      │  Identify issue       │                     │                    │
      │  Check git log        │                     │                    │
      ├──────────────────────>│                     │                    │
      │                       │                     │                    │
      │  Find last good       │                     │                    │
      │  commit (mobile-first)│                     │                    │
      │<──────────────────────┤                     │                    │
      │                       │                     │                    │
      │  git revert HEAD      │                     │                    │
      ├──────────────────────>│                     │                    │
      │                       │                     │                    │
      │  git push origin main │                     │                    │
      ├──────────────────────>│                     │                    │
      │                       │                     │                    │
      │                       │  Auto-deploy        │                    │
      │                       ├────────────────────>│                    │
      │                       │                     │                    │
      │                       │                     │  Deploy reverted   │
      │                       │                     │  state             │
      │                       │                     ├───────────────────>│
      │                       │                     │                    │
      │  ✅ Rollback complete │                     │                    │
      │<──────────────────────┴─────────────────────┴────────────────────┤
      │                                                                   │
      │  Verify mobile-first UI restored                                 │
      │<──────────────────────────────────────────────────────────────────┤
```

---

## Data Flow

```
┌──────────────┐
│   Browser    │
└──────┬───────┘
       │
       │ HTTPS Request
       │ GET https://indi-ka.vercel.app/
       │
       ▼
┌─────────────────────────────────────┐
│      Vercel Edge Network (CDN)      │
│                                     │
│  • Nearest edge location            │
│  • Cache check                      │
│  • Serve static assets              │
│  • Response headers:                │
│    - X-Build-Source                 │
│    - Cache-Control                  │
└─────────────┬───────────────────────┘
              │
              │ HTML + Assets
              │
              ▼
┌──────────────────────────────────────┐
│         Browser Rendering            │
│                                      │
│  1. Parse HTML                       │
│  2. Load CSS (globals.css)           │
│  3. Load JS bundles                  │
│  4. Execute React                    │
│  5. Render mobile-first UI           │
│  6. Console.log build info           │
└──────────────────────────────────────┘
              │
              │ User sees
              │
              ▼
┌──────────────────────────────────────┐
│      Mobile-First UI (Phone Mock)    │
│                                      │
│  • Header (Logo, Theme, Recent)      │
│  • Language Selector                 │
│  • Recording Interface               │
│  • Transcription Canvas              │
│  • History Sidebar                   │
└──────────────────────────────────────┘
```

---

## Configuration Hierarchy

```
Repository Root
│
├── .github/
│   └── workflows/
│       └── build-verification.yml ──────┐ CI/CD automation
│                                         │ Legacy UI guard
│                                         │ Build verification
│
├── vercel.json ─────────────────────────┐ Deployment lock
│                                         │ Main branch only
│                                         │ Cache headers
│                                         │ Build source header
│
├── next.config.ts ──────────────────────┐ Build configuration
│                                         │ Build ID from git
│                                         │ Strict mode
│                                         │ Clean builds
│
├── package.json ────────────────────────┐ Verification scripts
│                                         │ verify:ui
│                                         │ verify:build
│                                         │ deploy:check
│
└── src/
    └── app/
        ├── layout.tsx ──────────────────┐ Build metadata
        │                                 │ HTML meta tags
        │                                 │ Version stamping
        │
        └── page.tsx ────────────────────┐ Console logging
                                          │ Runtime verification
                                          │ Mobile-first UI
```

---

## Security & Integrity Model

```
┌─────────────────────────────────────────────────────────────────┐
│                    INTEGRITY GUARANTEES                          │
└─────────────────────────────────────────────────────────────────┘

1. SOURCE INTEGRITY
   ✅ GitHub is single source of truth
   ✅ Main branch is protected
   ✅ All changes are version controlled

2. BUILD INTEGRITY
   ✅ Build ID tied to git commit SHA
   ✅ Reproducible builds
   ✅ No manual build artifacts

3. DEPLOYMENT INTEGRITY
   ✅ Automated deployment from main only
   ✅ No manual server uploads
   ✅ Vercel locked to GitHub

4. RUNTIME INTEGRITY
   ✅ Version metadata in HTML
   ✅ Console verification
   ✅ Response headers confirm source

5. CACHE INTEGRITY
   ✅ Asset hashing prevents stale files
   ✅ Cache headers force revalidation
   ✅ CDN invalidation on deploy

6. ROLLBACK INTEGRITY
   ✅ Only mobile-first commits are targets
   ✅ Git history is immutable
   ✅ Automated rollback process
```

---

**Architecture Version:** 1.0  
**Last Updated:** 2026-02-11  
**Status:** Production Ready
