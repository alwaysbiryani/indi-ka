#!/bin/bash

# Local QA setup is intentionally disabled.
# Browser testing is handled by antigravity.

set -e

echo "ℹ️ Local QA setup is disabled for this repository."
echo "ℹ️ Do not run full QA agent workflows on local machines."
echo "ℹ️ Browser testing is handled by antigravity; CI runs use GitHub Actions."
